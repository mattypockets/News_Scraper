{{> nav/navbar-block}}
<div class="container">
    <!-- Div to display articles -->
    <div class="row">
        <div class="col s12">

            {{#each article}}
                {{> article/article-block}}
            {{/each}}

        </div>
    </div>
</div>  